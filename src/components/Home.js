import React from 'react';

function Home() {
  return (
    <section>
      <h1>Welcome to React on OpenShift!</h1>
      <p>This app is deployed with Jenkins, Docker, and Ansible.</p>
    </section>
  );
}

export default Home;
